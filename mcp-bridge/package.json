{
  "name": "shaderlab-mcp-bridge",
  "version": "1.0.0",
  "description": "MCP bridge for ShaderLab — translates MCP JSON-RPC to ShaderLab HTTP API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
