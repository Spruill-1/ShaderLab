<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ShaderLab.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShaderLab"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ShaderLab">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- File toolbar -->
        <Grid Grid.Row="0" Grid.ColumnSpan="3"
              Padding="8,4"
              Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:Name="SaveGraphButton" Content="Save Graph" />
                <Button x:Name="LoadGraphButton" Content="Load Graph" />
                <AppBarSeparator />
                <DropDownButton x:Name="AddNodeButton" Content="Add Node">
                    <DropDownButton.Flyout>
                        <MenuFlyout x:Name="AddNodeFlyout" />
                    </DropDownButton.Flyout>
                </DropDownButton>
                <AppBarSeparator />
                <Button x:Name="SaveImageButton" Content="Save Image" />
                <AppBarSeparator />
                <Button x:Name="EffectDesignerButton" Content="Effect Designer" />
            </StackPanel>
        </Grid>

        <!-- Left: Node Graph Editor area -->
        <Grid Grid.Row="1" Grid.Column="0"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="0,0,1,0">
            <Grid x:Name="NodeGraphContainer" Background="Transparent">
                <SwapChainPanel x:Name="NodeGraphPanel" />
            </Grid>
        </Grid>

        <!-- Column splitter (draggable) -->
        <Border x:Name="ColumnSplitter" Grid.Row="1" Grid.Column="1"
                Width="6"
                Background="{ThemeResource CardStrokeColorDefaultBrush}"
                PointerPressed="OnColumnSplitterPointerPressed"
                PointerMoved="OnColumnSplitterPointerMoved"
                PointerReleased="OnColumnSplitterPointerReleased" />

        <!-- Right: Preview + property tabs -->
        <Grid Grid.Row="1" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" MinHeight="180" />
            </Grid.RowDefinitions>

            <!-- Preview surface with toolbar and overlay -->
            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Preview toolbar: node selector -->
                <Grid Grid.Row="0" Padding="6,4"
                      Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      BorderThickness="0,0,0,1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Preview:"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"
                               Style="{StaticResource CaptionTextBlockStyle}" />
                    <ComboBox x:Name="PreviewNodeSelector" Grid.Column="1"
                              MinWidth="100" />
                    <TextBlock Text="Analysis:"
                               Grid.Column="2"
                               VerticalAlignment="Center"
                               Margin="12,0,8,0"
                               Style="{StaticResource CaptionTextBlockStyle}" />
                    <ComboBox x:Name="FalseColorSelector" Grid.Column="3"
                              MinWidth="120">
                        <x:String>None</x:String>
                        <x:String>Clipping</x:String>
                        <x:String>Luminance Zones</x:String>
                        <x:String>Out of Gamut</x:String>
                    </ComboBox>
                    <TextBlock Text="Display:"
                               Grid.Column="4"
                               VerticalAlignment="Center"
                               Margin="12,0,8,0"
                               Style="{StaticResource CaptionTextBlockStyle}" />
                    <ComboBox x:Name="DisplayProfileSelector" Grid.Column="5"
                              MinWidth="180" />
                    <CheckBox x:Name="CompareToggle" Grid.Column="6"
                              Content="Compare"
                              Margin="12,0,0,0" />
                </Grid>

                <!-- Compare toolbar (visible only when compare is active) -->
                <Grid x:Name="CompareToolbar" Grid.Row="1" Padding="6,4"
                      Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      BorderThickness="0,0,0,1"
                      Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Compare with:"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"
                               Style="{StaticResource CaptionTextBlockStyle}" />
                    <ComboBox x:Name="CompareNodeSelector" Grid.Column="1"
                              HorizontalAlignment="Stretch"
                              MinWidth="120" />
                    <TextBlock x:Name="SplitPositionText" Grid.Column="2"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0"
                               Text="Drag the split line on the preview"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </Grid>

                <!-- Swap chain panel -->
                <SwapChainPanel x:Name="PreviewPanel" Grid.Row="2" />

                <!-- Pixel trace crosshair overlay -->
                <Canvas x:Name="CrosshairCanvas" Grid.Row="2"
                        IsHitTestVisible="False" />

                <!-- Preview overlay (node name when not on Output) -->
                <Border Grid.Row="2"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="8,8,0,0"
                        Padding="6,2"
                        Background="#99000000"
                        CornerRadius="2"
                        Visibility="Collapsed"
                        x:Name="PreviewOverlayBorder">
                    <TextBlock x:Name="PreviewOverlayText"
                               FontSize="12"
                               Foreground="White" />
                </Border>
            </Grid>

            <!-- Row divider -->
            <Border Grid.Row="1"
                    Height="4"
                    Background="{ThemeResource CardStrokeColorDefaultBrush}" />

            <!-- Tab control for properties/editor/inspector -->
            <TabView x:Name="BottomTabView" Grid.Row="2"
                     IsAddTabButtonVisible="False"
                     TabWidthMode="SizeToContent">
                <TabViewItem Header="Properties" IsClosable="False">
                    <ScrollViewer>
                        <StackPanel x:Name="PropertiesPanel" Padding="12" Spacing="8">
                            <TextBlock x:Name="PropertiesPlaceholder"
                                       Text="Select a node to view its properties."
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabViewItem>
                <TabViewItem Header="Pixel Trace" IsClosable="False">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- Trace toolbar -->
                        <Grid Grid.Row="0" Padding="8,4"
                              Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                              BorderThickness="0,0,0,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="TracePositionText"
                                       Text="Click preview to trace a pixel"
                                       VerticalAlignment="Center"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       FontFamily="Cascadia Mono, Consolas, Courier New" />
                            <TextBlock Text="Unit:"
                                       Grid.Column="2"
                                       VerticalAlignment="Center"
                                       Margin="8,0,6,0"
                                       Style="{StaticResource CaptionTextBlockStyle}" />
                            <ComboBox x:Name="TraceUnitSelector" Grid.Column="3"
                                      MinWidth="100">
                                <x:String>scRGB</x:String>
                                <x:String>sRGB</x:String>
                                <x:String>Nits</x:String>
                                <x:String>PQ</x:String>
                            </ComboBox>
                        </Grid>

                        <!-- Trace tree with HDR swatch strip -->
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="28" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <SwapChainPanel x:Name="TraceSwatchPanel" Grid.Column="0" />
                            <ScrollViewer Grid.Column="1" Padding="4">
                                <StackPanel x:Name="PixelTracePanel" Spacing="2" />
                            </ScrollViewer>
                        </Grid>
                    </Grid>
                </TabViewItem>
            </TabView>
        </Grid>

        <!-- Status bar -->
        <Grid Grid.Row="2" Grid.ColumnSpan="3"
              Padding="12,4"
              Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="0,1,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="PipelineFormatText" Grid.Column="0"
                       Text="Pipeline: scRGB FP16"
                       Margin="0,0,24,0"
                       Style="{StaticResource CaptionTextBlockStyle}" />
            <TextBlock x:Name="DisplayModeText" Grid.Column="1"
                       Text="Display: SDR"
                       Margin="0,0,24,0"
                       Style="{StaticResource CaptionTextBlockStyle}" />
            <TextBlock x:Name="DisplayLuminanceText" Grid.Column="2"
                       Text="Max Luminance: --"
                       Margin="0,0,24,0"
                       Style="{StaticResource CaptionTextBlockStyle}" />
            <TextBlock x:Name="CursorReadoutText" Grid.Column="3"
                       Text=""
                       Margin="0,0,24,0"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       FontFamily="Cascadia Mono, Consolas, Courier New" />
            <TextBlock x:Name="FpsText" Grid.Column="5"
                       Text="FPS: --"
                       Style="{StaticResource CaptionTextBlockStyle}" />
        </Grid>
    </Grid>
</Window>
